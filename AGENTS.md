# Website publishing

When the user asks to update, publish, or sync this website, validate the relevant changes and push them to the `main` branch of `https://github.com/JanKazimierczak/Website.git`. Preserve existing repository history and avoid force-pushing unless the user explicitly requests it.
